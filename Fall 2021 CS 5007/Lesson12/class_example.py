class MyClass:
    def __init__(self):
        pass

    # Instance variable belongs to the objects of the class
    # They make up the internal state of the object
    # They can be initialized in the __init__(self) method

    # Public instance variable ---> self.name
    # Protected instance variable ---> self._name
    # Private instance variable ---> self.__name

    # Info Hiding & Encapsulation
    # Private instance variables/ methods are hidden and inaccessible from outside the class
